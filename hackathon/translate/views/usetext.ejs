<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ThinkSpace::Translate</title>
    <link rel="stylesheet" href="styles/main.css">
    <style>
        #backbtn {
                position: fixed;
                left: 20px;
                top: 20px;
                width: 60px;
                height: 60px;
                background-color: #fff;
                color: #333;
                cursor: pointer;
                border: 2px solid #333; 
                border-radius: 50%;
                transition: transform 0.3s ease, box-shadow 0.3s ease;
                font-size: 40px;
                display: flex;
                justify-content: center;
                align-items: center;
                box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.15);
            }
            #backbtn:hover {
                background-color: #333;
                color: #fff;
                transition: 0.3s;
            }
            a{
                text-decoration: none;
            }

         h1 {
            position: absolute;
            top: 25px;
            left: 50%;
            font-size: 42px;

            font-weight: 300;
            transform: translate(-50%, -50%);
            text-align: center;
            text-shadow: 2px 2px 2px rgba(0,0,0,0.2);
        }
    </style>
</head>
<body>
    <h1>ThinkSpace</h1>

    <div id="maintranslate">
        <a id="backbtn" href="http://localhost:5000/">&lt;</a>

    <form action="/usetext" method="post">


        
        <label for="content">Enter Content:</label>
        <textarea id="content" name="tcontent" placeholder="Type your content here" required></textarea>
        <br>
        <br>
        <label for="destinationLanguage">Select Language:</label>
        <select name="Languagecode">
          
            <% languages.forEach(language => { %>
                <option value="<%= language.code %>"><%= language.name %></option>
            <% }) %>
        </select>
        <br>
        <button type="submit">Translate</button>
    

    </form>
    <p>Result:</p>
    
    <div class="result" spellcheck="false" placeholder="Text will be shown here">
        <%= result %>
    </div>
</div>
<script src="languages.js"></script>
<script src="text_translate.js"></script>
</body>
</html>
